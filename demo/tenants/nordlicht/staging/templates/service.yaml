apiVersion: v1
kind: Service
metadata:
  name: {{ include "kaffee-shop.name" . }}
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: {{ include "kaffee-shop.name" . }}
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ .Values.nodePort }}
