# Git-Directory-Generator: jedes Verzeichnis unter apps/shop/overlays/ wird eine Application
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kaffee-git-dirs
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/bwiengarn/kaffee-shop-gitops.git
        revision: master
        directories:
          - path: apps/shop/overlays/*
  template:
    metadata:
      name: 'shop-{{ .path.basename }}'
    spec:
      project: nordlicht
      source:
        repoURL: https://github.com/bwiengarn/kaffee-shop-gitops.git
        targetRevision: master
        path: '{{ .path.path }}'
      destination:
        server: https://kubernetes.default.svc
        namespace: 'shop-{{ .path.basename }}'
      syncPolicy:
        syncOptions: [CreateNamespace=true]
