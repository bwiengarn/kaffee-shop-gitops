apiVersion: v1
kind: Service
metadata:
  name: kaffee-shop
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: kaffee-shop
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
